#ifndef _DEQUE_H_
#define _DEQUE_H_

#include <memory>

namespace my_deque {
    using namespace std;
    template<typename T>
    class deque {
        typedef size_t buffer_length;
        typedef T value_type;
        typedef T& reference;
        typedef T* pointer;


    };
}

#endif // !_DEQUE_H_
